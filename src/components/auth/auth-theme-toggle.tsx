"use client";

import { ThemeToggle } from "@/components/theme-toggle";

/** Client island: theme control for the login shell header. */
export function AuthThemeToggle() {
  return <ThemeToggle />;
}
